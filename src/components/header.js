// /*
//     React component - must start with a Capital letter
// */

// //! In the return line, if have multiple content then must include () to wrap in, otherwise don't need to.
// export default function Header() {
//     return <header className="container p-5">Header</header>;
// }

// JS function (after function always start with lower case)
// function sum(a, b) {
//     return a+b;
// }